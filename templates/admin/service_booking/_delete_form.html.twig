<form method="post" action="{{ path('app_bbr_admin_service_booking_delete', {'id': service_booking.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer cet équippement?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service_booking.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
