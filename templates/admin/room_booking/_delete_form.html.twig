<form method="post" action="{{ path('app_bbr_admin_room_booking_delete', {'id': room_booking.id}) }}" onsubmit="return confirm('Êtes-vous sûre de vouloir supprimer cet équippement?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ room_booking.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
