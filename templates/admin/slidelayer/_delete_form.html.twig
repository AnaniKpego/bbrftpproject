<form method="post" action="{{ path('app_bbr_admin_slidelayer_delete', {'id': slidelayer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ slidelayer.id) }}">
    <button class="btn btn-sm btn-danger">Delete</button>
</form>
