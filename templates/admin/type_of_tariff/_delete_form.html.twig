<form method="post" action="{{ path('app_bbr_admin_type_of_tariff_delete', {'id': type_of_tariff.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_of_tariff.id) }}">
    <button class="btn btn-sm btn-danger">Supprimer</button>
</form>
