<form method="post" action="{{ path('app_bbr_admin_room_category_delete', {'id': room_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ room_category.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
